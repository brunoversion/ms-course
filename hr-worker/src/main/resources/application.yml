server:
  port:
    8003
spring:
  application:
    name: hr-worker

  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: 
  
  h2:
    console:
      enabled: true
      path: /h2-console